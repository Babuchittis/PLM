_schema-version: 3.3.0
ID: PLM
description: A simple CAP project.
version: 1.0.0
modules:
  - name: PLM-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: PLM-auth
      - name: PLM-db
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: PLM-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: PLM-db
    parameters:
      buildpack: nodejs_buildpack
  - name: PLM-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: PLM-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - comsapplmpartnermanagement.zip
          name: comsapplmpartnermanagement
          target-path: resources/
        - artifacts:
            - project1.zip
          name: project1
          target-path: resources/
  - name: comsapplmpartnermanagement
    type: html5
    path: app/partnermanagement
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
  - name: project1
    type: html5
    path: app/project1
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: PLM-auth
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        oauth2-configuration:
          credential-types:
            - binding-secret
            - x509
        tenant-mode: dedicated
        xsappname: PLM-${org}-${space}
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
  - name: PLM-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: PLM-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: PLM-html5-service
      service-plan: app-host
  - name: PLM-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: PLM-destination-service
      service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
